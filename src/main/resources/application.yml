spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pallete?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: PalleteSecret123
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: update
  logging:
    level:
      org.hibernate.SQL: info

